# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1
      # - name: install flutter
      # - run: sudo git clone -b stable https://github.com/flutter/flutter.git $HOME/flutter
      # - run: export PATH="$HOME/flutter/bin:$HOME/flutter/bin/cache/dart-sdk/bin:${PATH}"
      # - run: echo "::add-path::$HOME/flutter/bin"
      # - run: echo "::add-path::$HOME/flutter/bin/cache/dart-sdk/bin"
      # - run: echo "$HOME/flutter/bin/cache/dart-sdk/bin:$HOME/flutter/bin" >> $GITHUB_PATH
      # - run: echo "$HOME/flutter/bin/cache/dart-sdk/bin:$HOME/flutter/bin" >> $GITHUB_PATH
      # - run: echo "$HOME/flutter/bin" >> $GITHUB_PATH
      # - name: test flutter
      - run: mkdir /opt/myscript
      - run: echo "echo aaaabbbbbcccc" > /opt/myscript/myscript.sh
      - run: cat /opt/myscript/myscript.sh
      - run: ehco "/opt/myscript" >> $GITHUB_PATH
      - run: echo "$PATH"
      - run: myscript.sh
      # - run: ls $HOME/flutter/bin
      # - run: .$HOME/flutter/bin/flutter
      - run: flutter doctor -v
      # - name: build web
      - run: flutter build web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_NO_LOADINGPAGE }}'
          channelId: live
          projectId: no-loadingpage
