# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # - name: install flutter
      # - run: export PATH="/opt/flutter/bin:/opt/flutter/bin/cache/dart-sdk/bin:${PATH}"
      # - run: echo "::add-path::/opt/flutter/bin"
      # - run: echo "::add-path::/opt/flutter/bin/cache/dart-sdk/bin"
      # - run: echo "/opt/flutter/bin/cache/dart-sdk/bin:/opt/flutter/bin" >> $GITHUB_PATH
      # - run: echo "/opt/flutter/bin/cache/dart-sdk/bin:/opt/flutter/bin" >> $GITHUB_PATH
      # - name: test flutter
      - run: mkdir /opt/myscript
      - run: echo "echo aaaabbbbbcccc" > /opt/myscript/myscript.sh
      - run: chmod +x /opt/myscript/myscript.sh
      - run: cat /opt/myscript/myscript.sh
      - run: echo "/opt/myscript" >> $GITHUB_PATH
      - run: echo "$PATH"
      - run: myscript.sh
      # - uses: subosito/flutter-action@v1
      - run: sudo git clone -b stable https://github.com/flutter/flutter.git /opt/flutter
      - run: echo "/opt/flutter/bin" >> $GITHUB_PATH
      - run: ls /opt/flutter/bin
      - run: ./opt/flutter/bin/flutter
      # - name: build web
      - run: flutter build web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_NO_LOADINGPAGE }}'
          channelId: live
          projectId: no-loadingpage
